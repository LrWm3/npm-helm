{
  "name": "@williammarsman/k8s-helm",
  "version": "0.1.1-latest",
  "description": "binaries for the helm command-line tool",
  "bin": {
    "npm-helm": "./src/index.js"
  },
  "dependencies": {
    "esm": "^3.2.25",
    "loglevel": "^1.6.4"
  },
  "scripts": {
    "test": "node src template test/template-test",
    "docker-build": "docker build . -t npm-k8s-helm:$npm_package_version"
  }
}